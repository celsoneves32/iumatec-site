/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.shopify.com",
      "images.alltron.ch",
      "media.digitec.ch",
      "static.digitecgalaxus.ch",
    ],
  },
};

export default nextConfig;